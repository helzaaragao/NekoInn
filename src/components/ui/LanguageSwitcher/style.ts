import styled from "styled-components";

export const LanguageButtonContainer = styled.aside`
  display: flex;
    
    button{
        width: 0.5rem;
        height: 0.5rem;
      
        span{
            font-size: 0.8rem;
        }
    }
`